# V.Canvas · 开发交接文档

> **本文档用途**：作为下一位开发者 / 后续开发会话的唯一开发依据。
> **文档状态**：需求方向已确认，技术细节已按 SillyTavern 1.18.0 真实源码核实。标注说明：⚠️ 表示需注意的实现细节或约束；「约束」表示硬性要求，不可修改；「已核实」表示已通过源码核实或实测。
> **当前进度**：**v0.1.0 + 二轮改造已完成并装入酒馆**，离线自测 **101/101** 通过。下个窗口**先读「〇·〇」**，再按需查阅〇至十五。
> 源码：`C:\shllll\V.Canvas\`；已安装副本：`<SillyTavern>\data\default-user\extensions\V.Canvas\`。
> 根计划文档（改动清单与变更记录）：`C:\shllll\开发计划.md`。
>
> **术语统一**：正文中的 `[ILLUST: 描述 | Danbooru,Tags]` 称为 **tag**（生成 tag），不称"标记/占位符"。代码中的标识符仍为 `marker*`，但面向用户的文案一律用 tag。
> "占位符"专指绘制期间在正文原位显示的那句「正在绘制插画」（纯 DOM，不落盘）。

---

## 〇·〇·〇、2026-09-17 V站专属密钥签名（**最新，先读本节**）

### 背景：V站 有两种密钥

| 密钥 | 形态 | V站如何处理 | 谁能用 |
|---|---|---|---|
| 通用密钥 | 40 位随机串，**无前缀** | 不验签，按**通用额度**扣减 | 任何 NAI 客户端（官方客户端、第三方中转、V.Canvas 都行） |
| 专属密钥 | `vcs_` + 随机串 | **必须验签**，失败直接 403，不降级 | 只有会算签名的客户端（即 V.Canvas） |

签名算法（V站 `lib/crypto.js` 与本站 `lib/vsig.js` 同一套）：

```
sig = md5(密钥 + UTC 日期 YYYY-MM-DD + 共享盐)
传递：请求头 X-V-Sig（V站 也接受 x-sig / body.sig / query sig）
服务端容忍 ±1 天（时区/时钟差异）
```

**盐由站点下发，不写进源码**：

1. 出图前，C 调 `GET {站点}/ai/user/subscription` 读 `v.salt`，即为该站点当前使用的盐
   （该接口不验签，通用/专属密钥都能查；结果按站点地址缓存，一次会话只问一次）；
2. **站点轮换 `exclusiveSalt` 后，C 下次请求自动跟随，不必更新插件版本** ——
   这是不把盐写死在源码里的根本原因（写死 = 换盐那天所有老插件集体失效）；
3. 若页面开着时站点刚换过盐，手里缓存的旧盐会被 403，此时自动清缓存重取新盐**重试一次**；
4. 源码里 `DEFAULT_EXCLUSIVE_SALT` 恒为空（公开仓库不含任何站点的盐，避免泄露）；
   取不到盐时**宁可不发签名**，也不发一个必定被拒的错值；
5. 扩展设置「专属签名盐」可手动指定，优先级最高（自部署实例 / 排障用）。

只有密钥是 `vcs_` 前缀才会走上面这套流程 —— 官方 NAI 与第三方中转**零额外请求**。

### 触发条件（**最小侵入，别扩大**）

**只有密钥以 `vcs_` 开头时才附加 `X-V-Sig`。** 其余情况 —— NovelAI 官方、
任何第三方中转、V站 通用密钥 —— 一律不加，请求与改动前逐字段相同。

原因：自定义请求头会触发 CORS 预检（OPTIONS），无差别加给所有上游会打断别人的服务。

### 本轮代码改动

| 文件 | 改动 |
|---|---|
| `lib/vsig.js` | **新增**。纯逻辑模块：MD5（浏览器端无 node:crypto，自带实现）、`utcDateStr`、`exclusiveSig`、`isExclusiveKey`、`sigHeaders`。不依赖酒馆，可 `node` 直接单测 |
| `lib/nai-api.js` | `generateIllustration` / `testConnection` 新增可选参数 `salt`、`signMode`；`fetch` 的 headers 里展开 `...sigHeaders(apiKey, salt, { mode: signMode })`。**请求体与其它请求头一行未动** |
| `lib/settings.js` | 新增两个设置字段：`exclusive_salt`（留空用内置默认）、`sign_mode`（`off`/`auto`/`on`，默认 `auto`）；配套 `SIGN_MODES` 常量与 normalize 校验 |
| `index.js` | 三处调用点（`drawMarkers` 出图、`test` 试连、`translate.generate` 转译出图）传入 `salt` 与 `sign_mode` |

`sign_mode` 三档：

| 取值 | 行为 |
|---|---|
| `auto`（默认） | 仅 `vcs_` 专属密钥附加签名；其余一律不加 |
| `off` | 任何密钥都不加（排障用；此时连 V站 专属密钥也会被 403） |
| `on` | 任何密钥都加（自部署 V站 想把通用额度一起锁住时用） |

### 验证（实测数据，非推测）

离线单测 26 项全通过（`vsig_test.mjs`）：

- MD5 与 `node:crypto` 对拍 11 组（空串 / 短串 / 中文 / 55·56·64·119·120 字节边界 / 1000 字符）逐一致
- 与 V站 `lib/crypto.js` 的 `exclusiveSig` 跨项目对拍：同一密钥同一盐算出**同一个值**，
  且 `verifyExclusiveSig` 认可本站产出的签名、拒绝错误签名
- 端到端拦截 `fetch`：官方 NAI 路径请求头仍为 `Accept,Authorization,Content-Type`（无多余字段）；
  开/关签名时**请求体逐字节相同**；完整 NovelAI 参数集（21 项）仍在

服务器实测（VPS <VPS>，线上正式站点）：

| 场景 | 结果 |
|---|---|
| 第三方·无签名 ×5 | `403` ×5 |
| 第三方·错误签名 ×5 | `403` ×5 |
| V.Canvas·正确签名 | 通过验签（进入转发上游） |
| 请求前后用量 | `general 5 / exclusive 1` → **完全未变** ✅ |

即：第三方一滴额度都消耗不掉 —— 验签在额度判定与转发之前，且 V站 只在上游 2xx 时计数。

### ⚠️ 注意事项

1. **MD5 摘要按小端字节序输出**（32 位状态字的低字节在前）。直接 `toString(16)`
   会得到每组 4 字节反转的错误结果 —— 本次就是踩了这个坑，已用 `DataView` 修正并加断言。
2. **前端可被逆向**：签名算法在浏览器里，理论上能被扒出来。方案文档的定位是
   「提高门槛，而非绝对禁止」；真出现绕过再升级为票据模式（先换短期票据再生图）。
3. 页面内直连（同页面 V.Adapter 桥）不经过 HTTP，**不需要也不发送**签名。

---

## 〇·〇、2026-09-15 插图显示修复（最新，先读本节）

### 症状

两个插件的出图均"生成成功但图片显示不出来"：落盘的 `.png` 实际只有 35 字节，
内容为 JSON 文本 `{"message":"页面内调用失败"}`，任何面板与正文自然都无法渲染。

### 根因（调用链逐级）

1. C 未填服务地址 → 走 A 的页面内调用桥 `window.__V_ADAPTER_NAI__`
2. 上游本次只返回**远程图片链接**，且浏览器因 CORS 无法下载字节 →
   A 的管线按既有设计降级为 `ImageResult(data=null, remoteUrl)`（该降级本身正确）
3. ⚠️ 缺陷一（A 侧）：桥把结果原样映射为 `{status:200, bytes:r.data}`，`data=null` 时
   **bytes 丢失**，返回了"200 但没有字节"的成功结果
4. ⚠️ 缺陷二（C 侧）：桥的调用方把「200 无字节」编码为错误 JSON `{"message":"页面内调用失败"}`
   却**保留了 status 200** → 响应被判为成功 → JSON 字节按图片流程 base64 落盘成 `.png`

HTTP 网关（:8888）链路不受影响：服务端下载无 CORS 问题，字节始终存在。
这也解释了老版本（无页面桥、只走 8888）没有此问题。

### 修复（3 处，桥协议补充 url 语义）

| 文件 | 改动 |
|---|---|
| `V.Adapter/index.js` | 桥识别 `data=null && remoteUrl`：返回 `{status:200, bytes:null, url:remoteUrl, via}` |
| `V.Canvas/lib/nai-api.js` | 桥分支新增两条判定：`200+url 且无字节` → `generateIllustration` 直接返回 `{url}`（调用方引用远程链接）；`200 无字节无url` → 明确报错，**不再生成 200 状态的错误响应** |
| `V.Canvas/index.js` | `drawMarkers()` 与 `translate.generate` 两处调用点：`gen.url ?? await saveBase64AsFile(...)`，远程链接结果不落盘、直接作为插图 URL |

桥协议（`__V_ADAPTER_NAI__` 返回值）自此为：

| 返回 | 语义 |
|---|---|
| `{status:200, bytes, contentType, via}` | 本地字节结果 |
| `{status:200, bytes:null, url, contentType, via}` | CORS 降级：只有远程链接，调用方直接引用 |
| `{status:非200, error}` | 失败，`error` 为可读原因 |

### 验证与同步

- 离线自测 109 项断言全通过（`ST_JSZIP=<酒馆>\lib\jszip.min.js node test\test.mjs`）
- 已同步安装副本：`extensions\V.Adapter-ST\index.js`、`extensions\V.Canvas-ST\index.js`、
  `extensions\V.Canvas-ST\lib\nai-api.js`（SHA256 一致），前端改动 Ctrl+F5 生效

---

## 〇·〇·二、2026-09-15 第二轮：管理面板手机端空白修复

### 症状

桌面端打开管理面板一切正常；手机端（Termux 本地部署酒馆 + 手机浏览器、
或应用内 WebView）打开后只见顶部的面板标题栏，面板内容完全不显示。
A、C 两插件表现一致。

### 根因（实测数据，非推测）

用 Chrome 移动视口模拟（412×915、安卓 UA、触摸）复现，拿到的实测值：

| 元素 | 修复前 | 修复后 |
|---|---|---|
| 浮层 `#*_panel_overlay` | **16px / 48px 高** | 915px |
| iframe `#*_panel_iframe` | **高度 0** | 871px |

进一步逐条探针定位到 **酒馆的移动端 CSS 把 `body` 设成了 `position: fixed`**。
在这个前提下，安卓 Chromium 对固定定位元素的解析与桌面端不同：

| 写法 | 移动端实测高度 |
|---|---|
| `position:fixed` + `top/bottom:0` | **0px**（浮层只剩内边距厚度） |
| `position:fixed` + `height:100%` | **0px** |
| `position:fixed` + `height:100vh` | 915px ✅ |
| `position:absolute` + 上下左右 0（父级为定位元素） | 915px ✅ |

因此浮层被压成一条细边，iframe 高度归零 → 面板内容全部落在屏幕之外。

另有一处独立缺陷：iframe 是**替换元素**，`width/height: auto` 会退回固有尺寸
**300×150** 并忽略左右偏移，即使浮层满屏 iframe 也只有 300×150。

### 修复

| 文件 | 改动 |
|---|---|
| `V.Adapter/style.css`、`V.Canvas/style.css` | 浮层改 `top:0; left:0; width:100%; height:100vh`（弃用 `top/bottom` 偏移与百分比高度）；iframe 改显式 `calc()` 宽高；窄屏（≤720px）浮层整屏、只留 44px 操作栏；操作栏按钮 `nowrap`、标题省略号 |
| `V.Adapter/index.js`、`V.Canvas/index.js` | 新增 `fitPanelHeight()`：按 `visualViewport.height`（回退 `innerHeight`）赋精确像素高度，监听 `resize` / `orientationchange` / `visualViewport.resize`，关闭面板时解绑 |
| 同上 | 浮层新增「新标签页」入口；iframe 载入 8 秒内未拿到内容时显示兜底提示，附「在新标签页打开面板」按钮 |
| `V.Adapter/index.js` | 面板从 `fetch + srcdoc` 改为真实 `src` 地址（srcdoc 在部分 WebView 受限） |
| `V.Adapter/panel.html`、`V.Canvas/panel.html` | 桥接解析兼容 `window.parent`（内嵌）与 `window.opener`（新标签页）两种打开方式；面板启动时对桥接做短暂重试，避免误报"未连接" |

### 版本

- V.Adapter：`v1.1.4-st.1` → **`v1.1.5-st.1`**（`index.js` / `lib/virtual-api.js` /
  `server-plugin/lib/server.js` / `server-plugin/package.json` / `manifest.json` 同步）
- V.Canvas：`0.1.0` → **`0.1.1`**（`index.js` / `manifest.json`）

### 验证

- 移动视口 412×915：浮层 412×915、iframe 412×871，两个面板均完整渲染（截图核对）
- 桌面视口 1440×900：浮层 1440×900、iframe 1404×844 / 1392×836，无回归
- 离线自测 109 项断言全通过

## 〇·〇·四、2026-09-15 第四轮：登录上游网关排障 + 出图链路默认改聊天

### 服务端实测（root 登录用户 VPS <VPS> 后取得）

| 检查 | 结果 |
|---|---|
| 账号池 `data.json` | 11 个号（`@pugb.vip` 临时邮箱），**token 全部有效**（209 字符，有效期至 2026-10-15） |
| 每号 `proxy` 字段 | **全部为 null** → 11 个号共用同一出口 IP |
| 各号用量 `stats` | 仅 2k~5k token，**远未到量**（可排除"额度用尽"） |
| `/api/accountStats` | 11 个号 status 均为 `active`，无冷却、无 lastErrorCode |
| `POST /v1/chat/completions` ×6 | **6/6 成功** |
| `POST /v1/images/generations` | **429**：`上游被阿里云 WAF 风控拦截（返回验证码页）` |
| `POST /v1/chat/completions`（让模型出图） | **200**，返回 `![image](https://cdn.qwenlm.ai/...)` |

结论：**聊天链路永久可用，且聊天链路本身就能出图**；标准生图接口被上游风控拦死。
账号轮询本身正常（LRU 选择"最久未使用的可用账号"），失败时网关**不会换号重试**
（`request.js` 的重试只针对传输层错误），所以单次请求撞上被拦接口就直接失败。

### 代码改动

| 文件 | 改动 |
|---|---|
| `V.Adapter/lib/settings.js`、`server-plugin/lib/settings.js` | `chat_fallback` 默认值 `auto` → **`chat_only`**（默认走聊天接口出图） |
| `V.Adapter/lib/pipeline.js`、`server-plugin/lib/pipeline.js` | 新增 `imagesEndpointUnavailable(err)`：按**上游网关自身的错误契约**判定"标准接口当前不可用"，计入熔断计数 |
| 同上 | `chat_only` 失败时补一句指路提示（若上游是生图模型请改回 auto/off），避免默认值把用户带坑 |
| `V.Adapter/panel.html`、`server-plugin/panel.html` | 设置中心下拉兜底值同步为 `chat_only` |
| `V.Adapter/README.md` | 生图链路章节与参数表改为"默认 `chat_only`"，并写明原因与切换方式 |

### 熔断判定的契约对齐（不再自行发明"风控"文案）

判定顺序（`imagesEndpointUnavailable`）：

1. **HTTP 状态**：`429` 或 `>= 500` → 视为标准接口当前不可用；
2. **结构化字段**：响应体 `error.code` ∈ {`upstream_waf_challenge`, `quota_limit`,
   `upstream_business_error`, `upstream_unavailable`} 或 `error.type` ∈ {`rate_limit_error`,
   `server_error`} → 不可用；
3. **文本兜底**（仅前两步未命中时）：只认网关自己使用的 WAF 特征词
   —— 与其 `chat.image.video.js` 里的 `WAF_BODY_RE` 同源：
   `aliyun_waf` / `AliyunCaptcha` / `FAIL_SYS_USER_VALIDATE` / `RGV587` / `captcha` / `验证码` / `阿里云 WAF`。

⚠️ **实测差异**：容器里实际运行的是 9 月 1 日构建的镜像，其风控响应为
`HTTP 429` + `{"error":{"type":"invalid_request_error","message":"上游被阿里云 WAF 风控拦截…"}}`
—— **没有 `code` 字段**（同仓库较新源码里有 `code: 'upstream_waf_challenge'`）。
因此当前版本**以 429 为主判据**，结构化 code 用于向前兼容，文本特征仅作兜底。

用户现有实例（`C:\677\...\settings.json`）的 `extension_settings.v_adapter.chat_fallback`
已由 `auto` 手工改为 `chat_only`，原文件备份为 `settings.json.bak-20260915-224922`。

---

### 现象

- 聊天里出图看起来正常，但面板（C 概览缩略图、C 生成记录、A 转译与测试预览）看不到图
- 出图时好时坏；A 面板偶发 `请求上游接口失败（…/v1/chat/completions）：Failed to fetch`

### 实测结论（本机浏览器跑通，日志原文）

```
[Upstream] 带 negative_prompt 请求失败（HTTP 429），去掉该字段重试
[Upstream] 标准生图未拿到图片（上游生图接口返回 HTTP 429：
  {"error":{"message":"上游被阿里云 WAF 风控拦截（返回验证码页），本次未生成图片。
   通常与账号/IP 触发风控有关：请稍后重试、放慢生图频率，或为账号配置代理出口。"}}），转聊天接口兜底 …
[Upstream] 图片因跨域限制无法本地处理，直接使用原始链接
[Translate] 出图成功（14466ms via images→chat兜底）：1024x1024 png 0KB
```

1. **"时好时坏"来自上游风控**：标准生图接口被阿里云 WAF 拦（HTTP 429）时转入聊天兜底，
   拦截是否发生取决于账号/IP 的风控状态，因此表现为"有时能出有时不能"。属上游账号侧问题，
   插件只能如实上报（`genLog` 里可见完整原因）。
2. **面板看不到图的真因：图片从未落盘**。聊天兜底返回的是上游 CDN 的**临时签名链接**
   （`https://cdn.qwenlm.ai/...png?key=<JWT>`），浏览器侧因跨域拿不到字节，管线按设计降级为
   「只有链接、没有字节」，落盘体积为 **0KB**。该链接的实测行为：
   `Referer` 为酒馆地址时 **403**、不带 Referer 时超时 → **随时间/来源变化即失效**，
   于是面板缩略图与旧消息中的插图都会变成裂图。
3. **`Failed to fetch` 与插件无关**：本机对同一上游实测——`/v1/models` 返回 401 且带
   `Access-Control-Allow-Origin: *`；`OPTIONS` 预检 204 且带 `access-control-allow-headers`；
   `POST /v1/chat/completions` 返回 **200 且带同一跨域头**。同一台机器上用浏览器跑 A 面板
   「仅转译文字」也成功。故该报错出在**用户设备到上游那一段**（安卓 WebView 默认禁止明文
   HTTP、或防火墙/网络路径），不是跨域配置问题。
4. **⚠️ 真因：上游 CDN 的防盗链只放行「无 Referer」或它自家站的 Referer。**
   用 curl 对同一链接逐项实测：

   | 请求头 | 结果 |
   |---|---|
   | 不带 Referer | **200**（1.74 MB 正常取回） |
   | `Referer: https://chat.qwen.ai/`（官方站） | **200** |
   | `Referer: http://127.0.0.1:8000/`（酒馆页面） | **403** |
   | `Referer: http://127.0.0.1:8000/scripts/extensions/.../panel.html`（面板页） | **403** |
   | 带 `Origin` | 200（Origin 不触发拦截） |

   正文里的插图能正常显示，是因为渲染该请求时不带 Referer；**面板里的 `<img>` 带了酒馆地址的
   Referer，于是必然 403 → 显示为裂图**。这正是「以前能用、现在全废」的直接原因：
   以前标准生图接口未被风控拦，拿到的是真实 b64 字节并落盘成本地文件，与 Referer 无关；
   现在只剩远程链接，是否可见就取决于 Referer 策略。

   **修复**：面板渲染图片时显式 `referrerpolicy="no-referrer"`（`V.Canvas/panel.html` 的
   `imgTag()` / `V.Adapter/panel.html` 的两处预览）。实测两引擎下图片均正常加载。
   注：浏览器侧 `fetch` 下载这些链接仍然不可行（跨域），**落盘仍建议走 8888 服务端形态**；
   但只加 `referrerpolicy` 就已能让面板把远程链接正常显示出来。

### 根治方向：让字节落到服务端

浏览器侧无解（跨域 + 不能被绕过）。可行做法是**在酒馆所在的机器上跑 A 的 8888 服务端形态**：

```sh
cd ~/SillyTavern/data/default-user/extensions/V.Adapter-ST
sh install-loader.sh          # Windows 用 install-loader.bat
```

此后上游调用与图片下载都由 Node 端完成（无跨域限制），C 把服务地址填成
`http://127.0.0.1:8888` 即可拿到**真实字节**并落盘，面板与旧消息从此不再依赖临时链接。

### 本轮代码改动（面板健壮性）

| 文件 | 改动 |
|---|---|
| `V.Canvas/panel.html` | 新增 `imgUrl()`：按酒馆根解析图片地址（兼容相对/绝对/远程/data，杜绝相对写法被解析到扩展目录）；缩略图去掉 `loading="lazy"` 改为立即加载；新增 `markImgErrors()`：加载失败显示红框提示并附原始地址；**远程链接（未落盘）的条目加角标**；失败红框里的地址做成**可点击链接**（点开即可判断是链接失效还是本机网络问题） |
| `V.Adapter/panel.html` | 同上：`imgUrl()` + `showImgError()`，作用于「测试连接」与「角色转译」的预览图 |

---

## 〇·〇·一、2026-09-14 二轮改造交接

本轮依据 `C:\shllll\开发计划.md` 实施了 6 项改动 + 1 项追加功能，并修复了 4 个真实 bug。
全部改动已同步至安装副本（逐文件 SHA256 核对一致），离线自测 **101 项断言全通过**。

### 1. 本轮落地了什么

| 项 | 内容 |
|---|---|
| 4.5 | 新增 `prompt_format`（auto/description/tags/both），按链路分流送出内容；规则文案重写为 7 条要求 |
| 4.7 | 点击插图弹出提示词浮层（单次捕获阶段委托监听，按需解析） |
| 4.1 | **移除 V.Adapter 的自动出图**（该功能归 2 号）；`/vgen`、角色转译、画风预设等保留 |
| 4.3 | 引擎配置以 V.Adapter 的 `:8888` 服务端为唯一权威；两个面板均标注作用范围 |
| 4.4 | V.Adapter 的 `/ai/generate-image` 新增 `?expand=1`；2 号新增「转译出图」页 |
| **4.9** | **「上下文出图」**：另行配置一个 OpenAI 兼容文字模型阅读正文与前文，产出提示词后交由出图上游作画。**不依赖剧情模型配合** |
| 附加 | 进度悬浮卡片（`lib/progress.js`，含「终止」）；规则注入位置新增 `inject_position` / `inject_depth` |

### 2. 现在是两条出图路线并存（架构关键）

```
角色回复生成完毕
  ├─ 正文里已有 [ILLUST: …]？
  │     └─ 是 → 标记驱动路线（processMessage，零额外等待）
  └─ 否，且启用了「上下文出图」
        └─ 交给分析模型（lib/llm-api.js → 独立配置的 OpenAI 兼容模型）
             └─ 产出 {desc, tags, anchor} → applyMarkers() 转成 tag
                  └─ 复用同一条出图 / 就地替换 / 状态记录链路（runIllustration）
```

两条路线**共用**同一套设置：出图上游、尺寸、`prompt_format`、`strip_marker`、**每轮上限**。
**不存在两套张数** —— 早期给上下文路线单开过 `ctx_max`，导致"设置页设 2 张实际只出 1 张"，已删除。

### 3. 设置项（26 个，面板全覆盖）

`test/contract.mjs` 硬性校验「桥接动作 ↔ 面板调用」「设置字段 ↔ 面板控件」两个集合必须一致
（字段清单从 `defaultSettings` 自动解析，非硬编码）。**新增设置后必须同步面板，否则它报错。**

新增的本轮字段：`prompt_format` / `inject_position` / `inject_depth` / `ctx_enabled` /
`ctx_url` / `ctx_key` / `ctx_model` / `ctx_style`（画风，可空＝按作品自动判定）/ `ctx_timeout_sec`。

### 4. 本轮踩的坑（下个窗口重点看）

**坑 1：标记驱动路线在「重度沙盒卡」上完全失效。**
实测（火影忍者 - 沙盒，153 条世界书条目）连续两轮 0 个 tag；换普通卡（Assistant）**同样 0 个**；
把规则注入改到 `in_chat` depth 0（与世界书模板同级）**依然 0 个**。
结论：不是某张卡的毛病，是路线的局限 —— 沙盒卡用 `状态栏`/`战斗思维链`/`世界观校验` 等大量
depth 0 世界书条目锁死输出格式，外部注入的规则一律被忽略。**这就是 4.9 存在的原因。**
（该卡还依赖 `ST-Prompt-Template` + `JS-Slash-Runner` + `SP·数据库` 三个扩展，用户已安装。）

**坑 2：提示词链路有四道关卡，每一道都可能静默吃掉图片。**
「模型产出 → 解析 → 拼成带语法的字符串 → Markdown 渲染」：

| 关卡 | 坑 | 修法 |
|---|---|---|
| 解析 | 模型输出包代码块/夹解释文字 | `parseAnalysisJSON()` 宽容解析 |
| 长度 | **`marker.js` 的 `MAX_MARKER_LEN`（现为 1200）超限整条丢，静默零报错** | `analysis.js` 的上限必须与之耦合（desc 700 + tags 400 + 外壳 ≈ 1114），并写注释钉死关系 |
| 语法 | `\|` `\]` 破坏 tag；**空格/括号破坏 Markdown URL** | `sanitize()` 清字符；`buildDisplayText()` 里 `encodeMdUrl()` 编码 |
| 显示 | `GENERATION_STARTED` 误清 `display_text`；URL 未编码导致图显示成纯文本 | `type === 'normal'` 不清；URL 编码 |

四个坑**全都实际踩过**， symptom 全都是"分析成功了但图没出来/不显示"，零报错。

**坑 3：URL 含空格 → Markdown 解析失败 → 图片显示成纯文本。**
出图文件落在以角色名命名的目录（如 `火影忍者 - 沙盒`，带空格），`![alt](url)` 解析失败。
已修：`encodeMdUrl()`。**旧消息会在重新载入聊天时自愈**（`rehydrateAll()` 重算 `display_text`）。
另：出图 URL 是 `/user/images/<角色名>/xxx.png`，**不是** `/api/images/`。

**坑 4：变量遮蔽。** `applyMarkers()` 外层有 `const tail = []`，循环里又写 `let tail = …`，
覆盖成字符串后 `tail.push is not a function`。改长函数前先扫一遍已有局部名。

**坑 5：同一语义不要开两个设置项**（见第 2 节末尾的 `ctx_max` 教训）。

**坑 6：前端文案必须中性技术措辞。** 口语化表达（"另配一个…让它…"、"即可"、"点上面的按钮"、
"它产出什么"）已被用户点名，全部改写；规则见项目记忆「文案风格」。

### 5. 本机环境与工具

- **无头浏览器验证**：`agent-browser` 装不上（且要下 500MB）。本机已有 Chrome/Edge，
  **用 CDP 直接驱动，零依赖**（Node 22 自带 `fetch` 与 `WebSocket`）。完整方法、六个坑、
  就绪判定条件、退出方式见项目记忆「无头浏览器驾驶」一节。
  ⚠️ **别往用户正在玩的聊天里插测试消息** —— 等于第二个客户端并发打同一上游，用户体感是"卡死"。
- **PowerShell 工具**：stdout 常被吞 → 结果写文件再 Read；`*>` 产出 UTF-16；
  `Remove-Item` 触发 safe-delete 报错（文件可能实际已删，先 `Test-Path`）；`cmd /c` 跑 `.bat` 可用。
- **`.bat` 同步脚本**：`copy` 目标**不能带尾部反斜杠**（`\"` 被当转义引号，报"拒绝访问"）；
  文件保持 ASCII-only，中文文件名用通配符（`*.md`）规避。

### 6. 当前状态与遗留

- 离线自测 **101/101**（`python test/make_fixtures.py` → `node test/test.mjs` → `node test/contract.mjs`）
- 已配置：`ctx_enabled=true`、分析模型 = `http://<VPS>:4000/v1` + `qwen3.8-max`、
  `max_per_round=2`、`debug=true`（便于排障，可在面板关闭）
- **已人工验证**：出图成功并就地插入（`/user/images/火影忍者 - 沙盒/illust_….png`）、
  进度卡片显示正常、点击插图可看提示词
- **待跟进**：
  1. 提示词质量依赖分析模型的能力；若画风/世界观仍判定不准，可在「上下文出图」页的
     **画风（可选）**里固定一项，或换更强的分析模型
  2. `debug` 目前为开（用户可在面板关）
  3. 用户的聊天里有若干轮由测试产生的「继续」消息（含一轮已正常出图的），是否清理由用户定
  4. 4.2（`nai_input_mode` 的保真改写档）仍为备选，未实施

---

## 〇、全局现状速览（新会话先读此节）

**两个插件相互独立，职责不同：**

| | 1 号：V.Adapter | 2 号：V.Canvas（本目录） |
|---|---|---|
| 是什么 | 出图引擎（OpenAI 上游 → NAI 协议 / 浏览器内出图） | 自动插画：AI 写 tag 即自动出图并插入聊天 |
| 形态 | **两个**：① 酒馆前端扩展 `..\V.Adapter\`（/vgen 那版，抽屉+面板）② 酒馆**服务端插件** `<SillyTavern>\plugins\V.Adapter\`（:8888 独立面板） | 酒馆前端扩展，仅此一个 |
| 抽屉 | 已精简：名称 + 版本 + 打开管理面板（功能全在面板） | 已精简：名称 + 版本 + 打开管理面板 |
| 面板 | 深色 dashboard（设计语言源头，2 号沿用） | 同一套设计语言，两页：概览 / 设置 |
| 1 号改动约束 | **未获明确授权不应改动**（本次需求中才调整） | 本项目 |

**酒馆侧现状（SillyTavern 1.18.0，本机 `127.0.0.1:8000`）：**

- 前端扩展：`third-party/V.Adapter`（1 号 UI 扩展）+ `third-party/V.Canvas`（2 号）—— 均被酒馆识别
- 服务端插件：`plugins/V.Adapter`（:8888 独立面板，`enableServerPlugins: true` 已开启）
- 三条出图链路均实测过：2 号 → V.Adapter → Qwen（17~22s，横图）、2 号 → NAI 网关直连（6.4s，832×1216 竖图）、
  1 号 → OpenAI 上游（自带聊天兜底）

**上游：**

| | Qwen 反代（日常） | NAI 网关（需高质量图时） |
|---|---|---|
| 地址 | `http://<VPS>:4000/v1`（经 V.Adapter） | `https://novelai-ln.minijiu.cfd`（直连，不经过 V.Adapter） |
| 模型 | `qwen3.8-max` | `nai-diffusion-4-5-full` |
| 特点 | 快、免费、接受自然语言，但尺寸不受控（1664×928 横图） | 竖构图、参数全生效、接受 Danbooru 标签；**每日 30 次 / 每分钟 2 次** |

**管理面板（本次新增）**：面板仅作为前端，后端仍为酒馆。面板运行于 iframe 内，为独立的 JS 世界，
**绝不能 `import '/script.js'`**（会重新初始化酒馆）——必须走桥接：面板发送指令 → 扩展执行（改数据/刷 DOM/落盘）→ 回执给面板。
可用性保障三机制见 13.9。**修改代码后需 Ctrl+F5 强制刷新酒馆**，否则缓存会导致改动看似未生效。

---

## 〇·一、后续接手待办（上一轮反馈与处理结果）

> 上一轮反馈（2026-09-14 11:47）：
> ① 面板左下角状态文案仅写「在线」，**不附加说明文字**（如"内置（随酒馆运行，无需独立端口）"）；该信息内部知晓即可，不应向最终用户展示。
> ② 1 号抽屉中的「打开管理面板」按钮**被挤压为竖排文字**（一字一行），需改为与 2 号一致的横排、占满整行、居中样式。

**两项均已修改并同步至酒馆：**

| # | 改动 | 状态 |
|---|---|---|
| ① | `V.Adapter\panel.html` 中 `#pill-text` 仅写 `'在线'`（原拼接了 `r.data.listen`） | 已改已同步 |
| ② | `V.Adapter\style.css` 为 `#v_adapter_open_panel` 增加 `width:100%; justify-content:center`（与 2 号的 `#v_canvas_open_panel` 写法一致），同时清理了抽屉中已失效的旧样式（status/field/checkbox/hint） | 已改已同步 |

**⚠️ 按钮竖排的根因尚未完全定位**（现象：按钮宽度被压缩至极小导致一字一行；增加显式 `width:100%` 后恢复）。
若后续 ST 更换主题后复现，优先检查 `#xxx_open_panel { width:100% }`，而非调整正则。

### 待确认（排队中）

1. **观感与交互需人工验证**：两个面板（1 号 / 2 号）的排版、按钮交互是否顺畅——本机无浏览器自动化，
   仅能保证语法、自测、动作契约一致，无法保证观感。
2. **2 号面板第二版功能**（待确认是否实现）：手动补图/重绘、画廊、tag 解析调试台、
   NAI 路径的画风前缀（画师串）、上游切换 + 真出图自检。
3. **GitHub 仓库未建立**。计划以链接方式安装，但需先将仓库推送上去 ——
   GitHub 用户名为 `VVVVVILK`，**旧 token 已失效需重新签发**；
   commit 邮箱使用 `<用户名>@users.noreply.github.com`，不应在公开提交中保留真实邮箱。
4. **手动重绘按钮**：已定项 6 明确第一版不做；若增加，应置于面板（而非抽屉）。
5. **另有一处待确认**：1 号面板设置中心「API 地址」栏写有
   `（内置扩展 · 无需配置地址）`——同类文案是否一并删除，待确认，不得擅自处理。

### ⚠️ 实现注意事项（基于实测）

- **`plugins/` 下的服务端插件必须有 `"type":"module"` 的 package.json**，否则被当作 CJS，
  酒馆仅输出一行 Failed to load 后照常启动、端口不开（参见 13.9）。
- **`V.Adapter\panel.html` 与 `server-plugin\panel.html` 已分叉**：
  二者的页面集合不同（扩展那份由酒馆页面内的扩展本体提供部分能力），**不应互相覆盖**。
- **共享 NAI 网关限流**（每日 30 次 / 每分钟 2 次）：应避免连续请求，**失败后停止**——
  连续请求会触发 Cloudflare 429，之后请求无法到达应用层，会将"被限流"误判为"参数错误"。
- **修改代码后必须 Ctrl+F5**，酒馆对扩展 JS/CSS 有缓存。

---

## 一、这个插件是做什么的

**概述**：在酒馆中与角色聊天 / 游玩剧情时，AI 每描述一个有画面的段落，插件即在**那段文字的正下方**就地插入一张生成插画——类似轻小说的图文竖排。

**效果示例**（AI 的回复）：

```
（AI 讲了一段剧情……）

[ILLUST: a silver-haired girl standing on a rainy neon street | 1girl, silver hair, cyberpunk, neon lights, rain]
```

→ 插件捕获标记 → 生成插画 → 标记被替换为：

```
（AI 讲了一段剧情……）

![Illustration](/api/images/xxxxxxxx.png)

（AI 后面接着讲的文字，被图挤到下面……）
```

### 图的插入方式（约束）

```
文字段落 A
──────────
文字段落 B      ← 标记出现在这里
──────────
   [ 插 画 ]     ← 图独占一行，插在段落正下方
──────────
文字段落 C      ← 被图"挤"到下面继续排
```

- **独占一行、垂直排列**：文字 → 图 → 后续文字
- **不应**左右浮动、不应文字环绕、不应并排、不应缩为行内小图
- **不应**将图统一追加到整条消息末尾（读者无法对应图与段落）
- 实现上使用 markdown 单行图片（天然块级），但需在 `style.css` 中兜底（见 6.9）

---

## 二、与 V.Adapter 的关系

| 资产 | 是什么 | 和本插件的关系 |
|---|---|---|
| **V.Adapter**（已完成的适配版） | 将 **OpenAI 格式转换为 NovelAI 格式**的服务 | 本插件**通过 NovelAI 协议指向它出图**——这是本插件唯一的上游通路 |
| **本插件**（本目录） | 酒馆内的**自动插画**前端扩展 | 仅负责"何时画、画完插在哪" |

### 约束：仅实现 NovelAI 协议，**不实现 OpenAI 兼容直连**

**不应自行增加该能力。** 理由：若本插件直接支持填写 OpenAI 地址直连上游，用户仅凭一个 Key 即可出图，
中间的协议转换服务将失去存在意义，V.Adapter 亦不再被使用。两个插件为**配套关系**：

- **V.Adapter** = 将 OpenAI 上游封装为 NovelAI 协议服务
- **本插件** = 消费该 NAI 协议服务，负责在聊天中自动插画

**运行前提**：本插件工作前，**必须有一个 NovelAI 协议服务在运行**（用户自己的 V.Adapter 适配服务，或任何 NAI 协议兼容服务）。设置中仅填其地址，默认 `http://127.0.0.1:8888`。

---

## 三、架构（数据流）

```
用户与 AI 对话
   │
   ▼
AI 回复里输出 [ILLUST: 英文描述 | Danbooru,Tag] 标记
   │
   ▼  监听 MESSAGE_RECEIVED（流式 / 非流式都会触发）
插件捕获标记 → 记录标记在原文中的精确位置
   │
   ▼  立即反馈：提示"正在绘制插画"（见 6.7，不应污染聊天记录）
调用 NovelAI 协议接口（指向用户的适配服务）
   │
   └─ 响应是 ZIP → 用 JSZip 解压 → 取首个 PNG
   │
   ▼  图片转 Base64 → saveBase64AsFile() 存进酒馆 → 拿到 URL
   ▼  把标记就地替换成 ![Illustration](url)（独占一行）
   ▼  updateMessageBlock() 刷新 DOM + saveChat() 持久化 → 完成
```

---

## 三·补充、上游实况（影响体验设计，**必读**）

上游为反代 Qwen 网页版的服务（V.Adapter 适配中配置的 `qwen_url`，默认 `http://127.0.0.1:4000/v1`）。以下为从 V.Adapter（Go 版）源码中记录的**实测事实**：

| 项 | 实况 |
|---|---|
| 标准生图接口 `/images/generations` | **稳定 500**（上游自身故障，与参数无关）→ 实际不可用 |
| 聊天接口 `/chat/completions` | **可用**：模型"画图并返回图片链接"，**单张约 30~60 秒** |
| 实际出图链路 | **聊天接口兜底**（适配服务已自动处理：先试标准接口约 0.3s 失败 → 转聊天） |
| 图片水印 | 聊天链路返回的图**带右下角 "Qwen" 角标** → 适配服务已自动抹除（插件端不用管） |
| 风控 | 存在**内容策略拒绝**（模型回"无法生成…"）与 **punish 验证页** → 适配服务已做可读报错 + 熔断（连续 3 次故障 → 30 分钟内直接走聊天） |
| 尺寸 | 上游拒绝尺寸时，适配服务会回退默认尺寸重试 |

### 设计要求（强制性）

1. **超时 ≥ 300 秒**（单张可能 60s+，2 张串行可达 2 分钟）
2. **多张串行生成，禁止并发**（并发易触发上游风控）
3. **等待提示应如实说明**：如"正在绘制插画（约 30~60 秒/张）"，避免用户误以为卡死
4. **不阻塞聊天**：生成在后台进行，用户可继续对话；不应锁死输入框
5. **错误信息应透传适配服务返回的 message**（该 message 已为可读文本：风控 / 内容策略 / 超时等）
6. **确认仅使用聊天窗口出图（`chat_only`）**：上游标准生图接口不可用（已确认），适配服务的 `chat_fallback` 设为 `chat_only`，直接走聊天接口出图（单张 30~60 秒；图带 Qwen 水印 → 由适配服务抹除）

### 测试环境（2026-09-14 实测可用）

- **上游 API 地址**：`http://<VPS>:4000/v1`
- **密钥**：`1`
- **已验证**：`GET /v1/models` 连通正常（列 40 个模型）
- **⚠️ 链路与模型的配对（约束，不可颠倒）**：
  - 适配服务 `chat_fallback` = **`chat_only`**（仅用聊天接口）
  - `qwen_model` 填**聊天模型**（如 `qwen3.8-max`）—— 其为"在聊天中画图并返回图片链接"的模型，有历史实测支撑
  - **不应**填 `-image` 后缀的模型（如 `qwen3.8-max-image`）：那是给**标准生图接口**（`/images/generations`）用的，本链路不用标准接口
  - 配置后通过适配服务面板的"测试连接"实测一次最稳妥
- **注意**：插件侧仅需填 NAI 服务地址 `http://127.0.0.1:8888`（本机适配服务），**插件无需知晓上述上游地址**（上游由适配服务负责）

### 本机已部署（2026-09-14，可直接用）

| 东西 | 位置 / 状态 |
|---|---|
| **V.Adapter 适配服务** | 使用**酒馆服务端插件形态**：`<SillyTavern>\plugins\V.Adapter\`，随酒馆一起启动/关闭，**无需单独运行 exe** |
| 源码 / 说明 | `C:\shllll\V.Adapter\server-plugin\`（`README.md` 中有安装、配置、实测记录） |
| 需要开的开关 | `<SillyTavern>\config.yaml` → `enableServerPlugins: true`（`enableServerPluginsAutoUpdate: false` 可省去启动时的 git 检查） |
| 当前配置 | `qwen_url=http://<VPS>:4000/v1`、`qwen_model=qwen3.8-max`、`chat_fallback=chat_only`、`nai_key=v-adapter-8888` |
| 管理面板 | `http://127.0.0.1:8888/`（运行总览 / 画风预设 / 角色转译 / 生成记录 / 设置中心） |
| 实测 | 出图 16.6s → 2.76 MB ZIP，内含 `image_0.png`；CORS 预检 204 + `Allow-Origin: *`，浏览器中的插件可直接调用 |
| 已弃用 | `C:\shllll\v-adapter-Go版-未启用\` —— Go 独立进程版（**会与插件争用 8888 端口，二者不应同时运行**） |

⚠️ **实测发现：走聊天链路时出图尺寸不受控。** 请求 832×1216（竖构图），上游返回的是
**1664×928（横构图）** —— 聊天模型仅"在聊天中画一张图"，完全忽略 `width`/`height`。
因此插件中的「分辨率」设置对本链路**基本无效**，属已知现象，非缺陷。

⚠️ **偶发失败属正常。** 上游为反代的网页版，偶尔返回一个无法下载的图片链接
（表现为 `502` + "聊天生图返回的图片链接均无法下载或解析"），**重试一次通常即可恢复**
（实测第一次失败、第二次成功）。适配服务自身也会对瞬时故障重试一次。

---

## 四、生图接口规格（**仅实现 NovelAI 协议**）

### 4.1 NovelAI 协议（唯一实现）

- **Endpoint**：`${baseUrl}/ai/generate-image`
  - baseUrl 由用户填写，**默认 `http://127.0.0.1:8888`**（用户的适配服务）；也允许填 `https://image.novelai.net` 之类的官方/第三方地址
- **Header**：`Authorization: Bearer ${apiKey}`
- **Payload**：
```json
{
  "input": "<Danbooru Tags>",
  "model": "<用户自定义模型字符串>",
  "action": "generate",
  "parameters": {
    "width": 832,
    "height": 1216,
    "scale": 6.0,
    "steps": 28,
    "uc": "<负面提示词 Tags>"
  }
}
```
- **响应处理**：**是 Binary ZIP**，必须解压：
```js
// 酒馆自带 JSZip（UMD），动态加载后会挂到 window.JSZip
if (!window.JSZip) await import('/lib/jszip.min.js');
const zip = await window.JSZip.loadAsync(await resp.blob());
const firstImg = Object.keys(zip.files).find(n => /\.(png|jpe?g|webp)$/i.test(n));
const b64 = await zip.files[firstImg].async('base64');
const dataUrl = `data:image/png;base64,${b64}`;
```
- ⚠️ **不应**自行通过 npm 安装 JSZip / 向扩展目录塞库文件——酒馆已有，路径 `/lib/jszip.min.js`。
- ⚠️ NAI 协议报错时返回的是 JSON `{"message": "..."}`（非 2xx），需将 message 展示给用户。

> ⚠️ **严禁硬编码任何模型名**（约束）。所有模型名由用户在设置中手填字符串，输入框仅给 placeholder 提示文本，**不应**给默认值。
>
> **不应**实现"OpenAI 兼容直连"选项——见第二条约束。

---

## 五、触发与解析（占位符模式）

### 5.1 System Prompt 注入

给用户**一键复制**的提示词（同时提供"插件开启时动态追加到 system prompt"的开关）。
当前文案见 `index.js` 的 `ruleTemplate()`，逐条写明：格式、描述段须覆盖的要素、标签段的形式与排序、
单个标记的自足性、多标记的分布要求。示例（每轮上限 2 张）：

```
若情节出现显著的场景切换、角色外观变化或高张力画面，请在对应段落正下方另起一行输出标记：
[ILLUST: 自然语言描述 | Danbooru,Tags]
标记须遵守下列要求：
1. 两段都必须填写，以 | 分隔，不得省略其中任一段。
2. 描述段用自然语言书写，须覆盖角色外貌特征、服装与道具、动作姿态、场景环境、构图与镜头、画风。
3. 标签段用英文 Danbooru Tag，以英文逗号分隔，按「主体、外观、服装、动作、场景、风格」的顺序排列。
4. 单个标记须自足：应包含该画面完整的主角特征与场景信息，不依赖其他标记补全。
5. 一轮输出多个标记时，各标记须针对不同的画面时刻，并分布在正文的不同位置（例如中段与末段），不得集中在一处，也不得重复描述同一画面。
6. 画面构思与剧情正文在同一次生成中一并完成，无需等待额外步骤。
单次回复最多输出 2 个标记，每个标记对应一张插画，分别插在各自段落正下方。
```

### 5.2 正则捕获（需容错）

```js
const ILLUST_RE = /\[ILLUST[:：]\s*([^|\]]+?)\s*(?:\|\s*([^\]]+?)\s*)?\]/i;
```

- **Group 1（自然语言描述）与 Group 2（Danbooru Tags）分别对应两类上游**：
  扩散模型（官方 NovelAI / NAI 网关）取 Group 2，OpenAI 格式上游（含聊天模型）取 Group 1。
  实际取用由 `prompt_format` 设置决定，组装逻辑在 `marker.js` 的 `resolvePromptMode()` / `selectPrompt()`。
- `findMarkers()` 返回的 `prompt` 字段为两段的兜底合成（Tags 优先），仅用于展示与日志，不参与出图内容选择。
- ⚠️ AI 可能输出变体：全角冒号、缺 `|`、多个空格、换行。正则需容忍；捕获不到则静默跳过并在控制台留日志（不弹窗打扰用户）。

### 5.3 位置记录与多标记处理

捕获后要记住每个标记在**原文中的起止下标**，替换时用它——实现"就地替换"，而非"追加到末尾"。

⚠️ 一轮最多 2 个标记（见设置项"每轮上限"）：正则使用全局匹配（`g`），按出现顺序**逐个**生成、逐个就地替换，全部替换完再统一 `updateMessageBlock` + `saveChat`。

---

## 六、SillyTavern 集成规范（已按 1.18.0 源码核实）

### 6.1 扩展必须自行启动（⚠️ 注意事项）

SillyTavern 的扩展加载器（`public/scripts/extensions.js`）**仅负责把 `<script type="module" src=...>` 挂载到页面，不会调用扩展导出的 `init()`**。因此：

```js
// index.js 末尾必须自己启动（官方系统扩展和成熟第三方扩展都是这个模式）
init().catch(err => console.error('[illust] 初始化失败:', err));
```

（未如此实现时表现为：扩展被识别但页面无任何渲染，且控制台无报错。）

### 6.2 事件监听（已核实）

```js
import { eventSource, event_types } from '/script.js';

eventSource.on(event_types.MESSAGE_RECEIVED, async (messageId, type) => { ... });
```

- 签名是 **`(messageId, type)`**；`type` 取值：`normal` / `swipe` / `append` / `continue` / `appendFinal` / `impersonate` / `extension` 等。
- **流式输出也能收到**：流式在 `onFinishStreaming` → `finalizeIntermediaryMessage()` 里同样 emit 这两个事件（已核实），无需特殊处理。
- `MESSAGE_RECEIVED` 与 `CHARACTER_MESSAGE_RENDERED` 基本成对触发。**推荐监听 `MESSAGE_RECEIVED`**（在渲染前触发，适合先改数据再渲染）。
- 只处理 `is_user !== true && is_system !== true` 的消息。

### 6.3 读取与更新消息（已核实）

```js
const context = getContext();          // from '/scripts/extensions.js'
const msg = context.chat[messageId];   // 原始消息对象

msg.mes = newText;                                  // ① 改数据
context.updateMessageBlock(messageId, { ...msg });  // ② 刷新这一条的 DOM
await context.saveChat();                           // ③ 持久化
```

- `context.updateMessage()` **不存在**（原始构想中的 API 有误）。正确的是 `updateMessageBlock(messageId, message)`，定义于 `/script.js`，已挂入 `getContext()`。
- `updateMessageBlock` 仅重渲染该消息 DOM，**不会再触发事件**（安全，不会死循环）。
- ⚠️ 其渲染时优先取 `message.extra.display_text`，无则使用 `message.mes`（见 6.6）。

### 6.4 图片存盘（⚠️ 不应将 base64 写入消息文本）

```js
import { saveBase64AsFile } from '/scripts/utils.js';

// 返回酒馆服务器上的图片 URL（形如 /api/images/xxx.png）
const url = await saveBase64AsFile(base64Data, subFolder /* 角色名或 '' */, fileName, 'png');
msg.mes = msg.mes.replace(markerText, `\n\n![Illustration](${url})\n\n`);
```

为何必须如此：将 `data:image/png;base64,...`（动辄数百 KB）直接写入消息文本，**聊天记录文件会膨胀、上下文 token 会暴涨**。先落盘取 URL，再将 URL 写入。

### 6.5 防重复触发（⚠️ 必做）

`MESSAGE_RECEIVED` 会在 **swipe、重新生成、切换聊天重渲染** 等场景再次触发。若不防护，将重复生图并产生额外开销。

```js
if (msg.extra?.illust_done) return;   // 已处理过，跳过
...
msg.extra = msg.extra || {};
msg.extra.illust_done = true;         // 成功后打标记（随聊天记录持久化）
```

- 失败时**不应**打标记（允许重试），但需防抖：同一 messageId 正在处理中则跳过。

### 6.6 上下文污染（可选优化）

插图 markdown 会进 `mes` → 进上下文 → 浪费 token。酒馆渲染优先用 `message.extra.display_text`，可考虑把插图放进 `display_text`（仅显示、不进 prompt）。⚠️ 待实测确认后再采用；来不及则先用 `mes`。

### 6.7 加载态（⚠️ 不应将 loading 写入聊天记录）

原构想是"将标记替换为 loading 文本 + updateMessage"，**这会持久化**：一旦生图失败/用户刷新，聊天记录中将永久保留"正在绘制场景插画…"，且无机制替换。

**推荐做法**：loading 仅改 DOM、不落盘——

- 等待期间用 `toastr.info('正在绘制插画…')` 提示；
- 图拿到后一次性将标记替换为图片。

（若坚持在正文内显示 loading，则插件启动时必须扫描并清理历史残留的 loading 占位。）

### 6.8 其他可用 API（均在 `getContext()` 里）

`addOneMessage(msg)` / `scrollOnMediaLoad()` / `characterId` / `name2`；全局 `toastr`、全局 `$`（jQuery 3.5.1）均可用。

### 6.9 图片显示样式（约束：必须独占一行）

markdown 单行图片本身渲染为块级元素，天然"文字→图→文字"竖排。但必须在扩展的 `style.css` 中显式兜底，防止主题将其压为行内：

```css
.mmes_text img { display: block; max-width: 100%; height: auto; margin: 8px auto; }
```

验收时肉眼确认：图将后续文字**挤到下一行**，非并排、非环绕。

---

## 七、目录结构与 manifest.json

符合第三方扩展规范（装在 `data/<user-handle>/extensions/<扩展名>/`，访问路径 `/scripts/extensions/third-party/<扩展名>/`）：

```
V.Canvas/                 ← 扩展目录名
├── manifest.json
├── index.js                      ← 入口（**末尾必须自启动**，见 6.1）
├── style.css                     ← 设置抽屉样式 + 6.9 的图片样式
├── panel.html                    ← 管理面板（独立前端，经桥接控制扩展本体）
└── lib/
    ├── nai-api.js                ← NovelAI 协议调用 + ZIP 解包
    ├── marker.js                 ← [ILLUST:...] 正则捕获与定位
    └── settings.js               ← 设置项读写（存 extension_settings）
```

```json
{
  "display_name": "V.Canvas",
  "loading_order": 110,
  "requires": [],
  "optional": [],
  "js": "index.js",
  "css": "style.css",
  "author": "VILK",
  "version": "0.1.0",
  "homePage": "",
  "auto_update": false
}
```

---

## 八、设置字段清单

| 字段 | 说明 |
|---|---|
| 总开关 | 启用 / 关闭本插件 |
| NAI 服务地址 | 默认 `http://127.0.0.1:8888`；**也支持填 NovelAI 官方地址**（`https://image.novelai.net`）或任何 NAI 协议兼容服务 |
| API Key | 手填（适配服务的 `nai_key`，或官方 NAI token） |
| 模型名 | **手填字符串，禁止硬编码默认值**（placeholder 可写示例） |
| 分辨率 | 宽 × 高（如 832 × 1216） |
| 负面提示词 | 对应 NAI 的 `negative_prompt`（实现细节见 13.2） |
| 采样步数 / CFG | 对应 `steps` / `scale` |
| 提示词注入 | "追加 ILLUST 规则到 system prompt"开关 + 一键复制按钮 |
| 每轮上限 | **默认 2 张** |
| 排除的消息类型 | 是否忽略 `swipe` / `impersonate` 等 type |
| 超时时间 | 默认 300s |
| 请求间隔 | 可选（规避连点与限流） |

---

## 九、异常处理要求

- API 报错 / 超时 / ZIP 解析失败 / 返回不是图片：
  - 移除 loading 占位（恢复纯文本，**不残留文本**）
  - `toastr.error()` 弹出可读错误（带上上游返回的 message 片段）
- 标记解析失败：静默跳过，控制台留日志
- 拿不到图片 URL：可用 `data:` URL 兜底（仅当体积可控）

---

## 十、验收清单（完成后逐条勾选）

- [ ] 扩展在酒馆扩展面板出现，抽屉可展开（自启动生效）
- [ ] 设置项可保存，刷新后保持
- [ ] 代码中搜不到任何硬编码模型名
- [ ] 代码中**没有** OpenAI 兼容直连的实现（只走 NAI 协议）
- [ ] AI 回复含 `[ILLUST: a | b]` 时，图**就地插在该段落正下方**
- [ ] 图**独占一行**：文字→图→文字 竖向排列，无浮动、无文字环绕
- [ ] 首次回复 / 续写（continue、append）/ swipe 重新生成 都能正确触发
- [ ] 流式输出模式正常工作
- [ ] 同一消息不重复生图（切聊天、刷新、重渲染都不重复）
- [ ] 刷新页面 / 重开聊天后，插图仍在且位置正确
- [ ] NAI ZIP 能正确解压显示
- [ ] 上游报错时：loading 消失、弹窗提示、聊天记录干净
- [ ] 消息文本里**没有**超长 base64（图片已落盘成 URL）

---

## 十一、已定项与待定项

### 已定（已确认，直接照做）

1. **NAI 服务地址默认 `http://127.0.0.1:8888`**；同时允许填 **NovelAI 官方地址**或任何 NAI 协议兼容服务
2. **仅做 NovelAI 协议，不做 OpenAI 格式**（约束，不再讨论）
3. **每轮最多 2 张插画**
4. **插图不进上下文**：插图写入 `extra.display_text`（仅给用户看，不占 AI 记忆/Tokens），`mes` 保持纯文本
5. **swipe 换回复时重新配图**（否则图与新剧情不符），做成开关、默认开启
6. **第一版不做"手动重绘"按钮**（后续需要再加）
7. **命名已定稿**：显示名与扩展目录名均为 `V.Canvas`；设置存储键为 `v_canvas`（旧键 `v_illust` 首次启动时自动迁移）

---

## 十二、参考资料（SillyTavern 1.18.0 已核实源码位置）

| 事项 | 位置 |
|---|---|
| 扩展加载逻辑 | `public/scripts/extensions.js`（`activateExtensions` / `addExtensionScript`） |
| 事件触发点 | `public/script.js`：流式 `finalizeIntermediaryMessage()` ~L3740；非流式 `saveReply` ~L6632 |
| 消息更新函数 | `public/script.js` `updateMessageBlock()` ~L1974 |
| 图片存盘 | `public/scripts/utils.js` `saveBase64AsFile()` ~L1652（POST `/api/images/upload`） |
| context API 全表 | `public/scripts/st-context.js` `getContext()` ~L114 |
| JSZip | `public/lib/jszip.min.js`（UMD，动态 import 后挂 `window.JSZip`） |
| 事件枚举 | `public/scripts/events.js` |
| 官方生图扩展参考 | `public/scripts/extensions/stable-diffusion/index.js`（媒体/消息写法以此为准最稳妥） |

---

**接手注意事项**：标注「已核实」的 API 均来自真实源码，可直接使用；标注「⚠️」为已验证的注意事项，须遵守；标注「约束」为硬性要求，不可修改。代码完成后按"十、验收清单"逐条自测。

---

## 十三、实现说明（v0.1.0，代码已写完）

### 13.1 交付物

```
C:\shllll\V.Canvas\            ← 源码（唯一真源）
├── manifest.json                      display_name「V.Canvas」/ loading_order 110
├── index.js                           入口：监听 / 出图串行队列 / 就地替换 / 抽屉 UI / prompt 注入
├── style.css                          .mes_text img 独占一行 + 抽屉样式 + 出图进度条
├── README.md                          给用户看的安装/配置/使用说明
├── sync-to-sillytavern.bat            一键同步到酒馆扩展目录（改完源码跑一下）
├── lib\
│   ├── marker.js                      findMarkers / buildDisplayText / stripMarkers / effectiveSource
│   ├── nai-api.js                     NAI 协议调用 + ZIP 解包（含 JSZip 动态加载）
│   └── settings.js                    设置校验、归一化、持久化
└── test\                              离线自测（不依赖酒馆）
    ├── test.mjs                       39 项断言，起本地假 NAI 服务跑全链路
    ├── make_fixtures.py               生成测试用 PNG / ZIP
    └── report.txt                     最近一次结果
```

安装副本（`sync-to-sillytavern.bat` 的产物）：
`C:\shllll\SillyTavern-release\SillyTavern-release\data\default-user\extensions\V.Canvas\`

### 13.2 ⚠️ 与文档的两处必要偏差（有依据）

**① 负面提示词：文档写 `uc`，实现中 `negative_prompt` 与 `uc` 同发。**

文档 §4.1 的 payload 示例把负面词放在 `parameters.uc`。但 V.Adapter 的**真实源码**读的是
`parameters.negative_prompt`（以及 v4 的 `v4_negative_prompt.caption.base_caption`），
见 `V.Adapter\server-plugin\lib\nai.js` 的 `strFromMap(params, 'negative_prompt')`。
也就是说**仅发 `uc` 时适配服务收不到负面词**。

因此实现中两个字段均带同一值：适配服务认 `negative_prompt`，官方 NAI 也认
`negative_prompt`，旧客户端口径的 `uc` 一并携带无害。

**② 正文处理：`mes` 中剔除标记，真正的"含标记原文"存于 `extra.illust.src`。**

文档 §5.3 说"记住标记在原文中的起止下标，替换时用它"。实现保留了"就地替换"的语义，
但将状态拆分为两份：

| 位置 | 内容 | 用途 |
|---|---|---|
| `extra.illust.src` | **含标记的完整原文** | 显示与重试的唯一真源 |
| `extra.illust.urls` | 与标记一一对应的图片 URL（`null` = 这张还没出） | 每张单独记账，失败不重画已成的 |
| `extra.display_text` | src 把标记换成 `![Illustration](url)` 的结果 | 仅给用户看，**不进 prompt** |
| `mes` | src 剔除标记后的纯文本 | 进上下文的就是它（已定项 4） |

好处：切聊天 / 切 swipe / 刷新后能从 `src + urls` 随时重建显示；某张失败也能单独重试。

### 13.3 ⚠️ 文档未覆盖、但不实现会导致缺陷的两处

**① 生成开始时必须清除旧的 `display_text`。**
酒馆渲染时 `display_text` 的优先级**高于** `mes`（`script.js` L1977 `getLatestMessageText` 路径）。
续写 / 重 roll 时 `mes` 在变、`display_text` 还是旧的 → 流式内容根本看不见。
因此监听 `GENERATION_STARTED`，把**最后一条**配置过图的消息上的 `display_text` 先清除。

**② 续写（continue / append）时酒馆执行 `mes += 新文`，而标记已被剔除。**
不处理则 `src` 对不上，标记与 URL 的下标会错位（旧图被当新图用，或已出的图重画）。
`marker.js` 的 `effectiveSource()` 专门识别此情况，返回 `new / same / append / reset` 四态：
仅 `same`（正文未变）和 `append`（后面续写）复用已出的图，`reset`（swipe 到另一条回复 /
手动改过正文）一律作废旧状态重新配图。

### 13.4 已按文档实现的关键点

- 末尾自启动（`init().catch(...)`），不写将导致"扩展被识别、页面无渲染"
- 监听 `MESSAGE_RECEIVED (messageId, type)`，非流式与流式均触发（流式在 `onFinishStreaming` 走同一处 emit）
- `context.updateMessageBlock(messageId, {...msg})` + `context.saveChat()`，不使用不存在的 `updateMessage`
- `saveBase64AsFile()` 落盘取 URL，消息中仅有 `/api/images/xxx.png`，**无 base64**
- ZIP 使用酒馆自带的 `/lib/jszip.min.js` 动态 import（不塞库文件、不装 npm 包）
- 防重复：`extra.illust_done` + `inFlight` 集合 + 4 秒冷却；swipe 由酒馆自行还原 `extra`，故切换回复不会误耗额度
- loading 仅走 toastr + `.v_canvas_busy` 进度条（纯 DOM），失败后聊天记录干净
- 错误信息透传上游 `message`（NAI 协议非 2xx 的 `{"message": "..."}`）
- 超时默认 300s、多张**串行**、每轮上限 2
- 代码内搜不到任何硬编码模型名（`model` 默认空串，仅 placeholder）（约束）
- 无 OpenAI 兼容直连的任何代码路径（约束）

### 13.5 额外补充（不改变行为，仅便于排障）

- 抽屉中的「测试连接」按钮：请求 `GET {base}/ai/user/subscription`，沿用 NAI 协议的订阅端点
- 「控制台详细日志」开关：`[V.Canvas]` 前缀的 debug 日志

### 13.6 响应处理：优先直出图片，不再多套一层壳（需求）

**说明**：ZIP **并非本插件附加的封装层**，它是 **NovelAI 协议的响应格式**
（`POST /ai/generate-image` 官方即返回 ZIP）。这是项目自身的设计选择——对外仅认 NAI 协议面。
因此"解压"从来不是多余步骤，而是协议本身要求。

但**本扩展作为客户端时无需额外嵌套该层**。已改为：

| 响应 | 插件怎么处理 | 走不走解包 |
|---|---|---|
| 二进制图片流（`Content-Type: image/*`） | 直接取字节 | **不解包** |
| 裸图字节（Content-Type 不准） | 按魔数识别 | **不解包** |
| ZIP | 酒馆自带的 `/lib/jszip.min.js` 解出第一张 | 解包（兼容分支） |
| JSON | 取 `message` 抛错；防御性也认 `b64_json` | — |

服务端配合改动一处（`V.Adapter` 的 `lib/nai.js` 新增 `wantsRawImage()`）：
客户端声明只要图片（`Accept: image/*` 或 `?raw=1`）时，**直接返回 PNG/JPEG 字节流，不套 ZIP**；
不带 `Accept`、或显式要 `application/zip` 的客户端（酒馆助手等）仍拿到 ZIP。

实测（2026-09-14）：
- `Accept: image/*` → `Content-Type: image/png`，2.57 MB 裸 PNG
- 不发 `Accept` → `application/zip`，2.73 MB（协议面未破坏）
- `?raw=1` → 裸 PNG

⚠️ **为何不使用 `URL.createObjectURL(blob)` 作为最终地址**：object URL 是**临时的**，
刷新页面即失效。而验收第 10 条要求"刷新后插图仍在"，因此取得字节后必须
`saveBase64AsFile()` 落至酒馆的 `/api/images/xxx.png`，消息中存该稳定 URL。
`URL.createObjectURL` 只能作为"立即显示"的临时手段，用在此处省不到什么，反而丢失持久化。

### 13.7 绘制中的占位符（改为正文原位显示）

原实现使用右下角 toastr + 消息下方一条进度条。现按需求改为**在正文内 tag 的原位**显示
「正在绘制插画 … 约 30~60 秒」。

实现：`showPlaceholder()` 在已渲染的 `.mes_text` 中找到包含 tag 的最内层元素（那个 `<p>`），
将其内容替换为 `.v_canvas_placeholder` 元素。出图完成后 `updateMessageBlock` 整条重渲染，
占位符自然被真图替换；失败时 `finishMessage` 也会重渲染一次，占位符消失、tag 原样保留于正文。

⚠️ **绝不写入 `mes`**——写入后一旦失败/刷新，聊天记录中将永久保留"正在绘制"文本（见 6.7）。

### 13.8 NAI 网关实测，及由此改动的 payload（重要）

使用真·NAI 网关（`https://novelai-ln.minijiu.cfd`，模型 `nai-diffusion-4-5-full`）实测，
**纠正了此前的一个假设**：

> 旧认知：`input + model + action + parameters{width,height,scale,steps,negative_prompt}`
> 即合法 NAI 请求。
> 实测：该最小集会被网关返回 `400 {"message":"无效的请求"}`。
> 网关强校验参数完整性。补上 `params_version` / `sampler` / `noise_schedule` /
> `n_samples` / `ucPreset` / `qualityToggle` / `cfg_rescale` / **`v4_prompt`** /
> `v4_negative_prompt` 等一整套后才通过。

因此 `lib/nai-api.js` 现发送**完整的 NAI 4.x 参数集**（含 `v4_prompt.caption.base_caption`
= input，为 4.x 真正主输入）。该 payload 同时喂得动三种上游：
官方 NAI / NAI 网关 / V.Adapter（后者仅读 input 与 width/height/negative_prompt，多余字段忽略）。

**随后移除了 `uc`。** 此前因文档 §4.1 写 `uc` 而 V.Adapter 读 `negative_prompt`，故二者同发；
现确认 `uc` 对任何真实上游均无用（V.Adapter 亦不读它），保留反而增加被严格网关挑刺的风险。
现仅发 `negative_prompt` + `v4_negative_prompt`。

其他实测事实：

| 项 | 结果 |
|---|---|
| 连通性 | **直连可用**，不必挂代理（文档"建议代理"但非必需） |
| 出图耗时 | **6.4 秒**（比 Qwen 链路的 17~22 秒快得多） |
| 状态码 | **201**（不是 200）—— 用 `resp.ok` 判 2xx 即无碍 |
| 响应 | ZIP，内容物是 **`image_0.webp`**（非 png！`extFromName` 已支持 webp） |
| 尺寸 | 请求 **832×1216 通过**，回来正好 832×1216 —— 插件的竖构图默认值无需改 |
| 画质 | 正经的动漫插画，比 Qwen 聊天链路那条（1664×928 横构图）**更适合轻小说插图** |
| `GET /`、`/health`、`/ai/user/subscription` | **一律 403**（Cloudflare）。网关「允许范围」只有 `generate-image` |
| 配额 | 每日 30 次 / 每分钟 2 次 —— **自动插画是重负载，务必将每轮上限压到 1** |

因最后一条，插件为「测试连接」按钮的失败提示补充一句说明：
**不少网关仅放行出图接口，订阅端点被屏蔽，此处失败不代表不可用，以生成记录为准。**
（这也对应网关文档中"不要用插件的测试连接判断能不能用"的描述。）

⚠️ NAI 4.5 基于 Danbooru 系数据训练，因此**画师串 / Danbooru 标签**为其最佳输入形态——
结论：只要 AI 输出 `描述 | 标签` 双段，插件取标签喂 NAI，质量即有保障；
需观察的是 AI 是否遗漏 `|` 分隔符。

### 13.9 管理面板：抽屉瘦身 + 独立前端 + 桥接（需求）

需求：抽屉仅保留插件名、版本号、打开管理面板按钮；面板仅作为前端，后端仍为酒馆。

**改动：**

```
抽屉（酒馆内联）              管理面板（全屏浮层里的独立网页）
┌───────────────────────┐    ┌─────────────────────────────────────┐
│ V.Canvas v0.1.0│    │  概览  │ 上游 / 触发规则 / 本聊天出图   │
│   [ 打开管理面板 ]        │ →  │  设置  │ 全部 17 个字段 + 规则文本      │
└───────────────────────┘    └─────────────────────────────────────┘
```

- **抽屉仅剩三样**：插件名 + 版本号 + 「打开管理面板」按钮。总开关也移入面板。
- **面板 = `panel.html`**：设计语言**沿用 1 号（V.Adapter）的面板**（指定），
  同一套深色 dashboard；但**不跟随酒馆主题**（要求：面板不跟随酒馆主题，保留既有设计）。
- 面板**仅做 UI**，不触碰酒馆数据。

**⚠️ 桥接：确保操作真正生效的机制**

面板运行于 iframe 内 —— 其**无法获取 `getContext()`**，也**绝不能** `import '/script.js'`：
iframe 为独立的 JS 世界，若在其中加载 script.js，会将整个酒馆**重新初始化一次**
（出现两套 chat、两套 eventSource），属严重故障。

因此规则为「**面板仅负责 UI，扩展负责执行**」：

```
面板点按钮
  → window.parent.__V_CANVAS_API__('settings.patch', {enabled:true})
  → index.js 里的 installBridge() 真正执行：applyPatch() 改数据 + persist() 落盘 + syncPromptInjection()
  → 回 { ok:true, data:{ changed:[...], notes:[...], settings:{...} } }
  → 面板将结果显示出来
```

动作仅 6 个，面板与桥接一一对应（已用静态检查卡死）：

| 动作 | 作用 |
|---|---|
| `state` | 概览数据：开关、上游、脱敏 Key、**从 chat 现读的已出图列表** |
| `settings.get` | 全量设置 |
| `settings.patch` | 写设置（复用 `applyPatch`，返回真实 changed 列表） |
| `settings.reset` | 恢复出厂 |
| `rule` | 当前 prompt 规则文本 |
| `test` | 测试连接 |

**可用性保障三机制**：
1. 面板启动先自检 `window.parent.__V_CANVAS_API__` 是否存在 → 不存在则**显示红字提示并禁用写操作**，
   避免用户在面板无响应时空操作；
2. 每个动作**必有回执**（`{ok,data}` / `{ok:false,error}`），面板显示"已保存并生效"或错误原文；
3. 写设置收尾必走完 `applyPatch → persist(saveSettingsDebounced) → syncPromptInjection` 三步。

**新增的静态检查**（`test/contract.mjs`，防止"动作名对不上导致按钮无反应"）：

- 面板 `call('...')` 的动作集合 == 桥接 `case '...'` 的集合（不多不少）
- 面板中每个设置字段都有对应控件 id
- **`settings.js` 中 17 个字段全部被面板覆盖** → 即"纯搬家、功能一致"的机器证明

实测：`panel.html` 在 `/scripts/extensions/third-party/V.Canvas/panel.html` 返回 200（30762 字节）。

**⚠️ 待人工验证**：面板的观感与交互需人工在浏览器中确认 ——
本机无浏览器自动化，仅能保证契约、语法、动作集合一致，无法保证观感。

---

## 十四、验证记录（2026-09-14）

### 14.1 离线自测：39 / 39 通过

```
cd C:\shllll\V.Canvas
python test\make_fixtures.py     # 生成 1×1 PNG + 含该 PNG 的 ZIP
node   test\test.mjs             # 起本地假 NAI 服务，跑全部断言
node   test\contract.mjs         # 面板动作集合 vs 桥接动作集合的静态一致性检查
```

覆盖：

| 组 | 断言数 | 内容 |
|---|---|---|
| `marker.js` | 16 | 标准标记 / 全角冒号 / 缺 `\|` / 多余空格 / 标记内换行 / 多标记编号 / 就地替换位置 / 失败标记原样保留 / strip 幂等 |
| `effectiveSource` | 6 | new / same / append（续写重接原文且下标不错位）/ reset（swipe 到另一条回复） |
| `nai-api.js` | 17 | ZIP→base64 / 裸图字节 / 200+JSON 错误 / 非 2xx 透传 message / 空响应 / 连不上 / 未配置地址 / payload 形状（含完整 NAI 4.x 参数集与 `v4_prompt`）/ 测试连接 |

### 14.2 静态与语法

- `node --check`：`index.js`、`lib/*.js` 全部 exit 0
- 所有跨模块 import 的具名导出逐个在 1.18.0 源码中核对过：
  `/script.js` → `eventSource` `event_types`（L297-333 的 re-export 块）`setExtensionPrompt`(L8866) `extension_prompt_types`(L483) `extension_prompt_roles`(L493)
  `/scripts/extensions.js` → `getContext` `extension_settings`
  `/scripts/utils.js` → `saveBase64AsFile`(L1652)
  `getContext()` 返回体 → `chat` `name2` `updateMessageBlock` `saveChat` `streamingProcessor` `scrollOnMediaLoad`

### 14.3 真机（SillyTavern 1.18.0 本机实跑）

本机将酒馆跑起来实测（`node server.js`，`127.0.0.1:8000`）：

| 检查 | 结果 |
|---|---|
| `GET /api/extensions/discover` | 200，返回 15 条，其中 **`type=local name=third-party/V.Canvas`** ，服务端已识别 |
| `/scripts/extensions/third-party/V.Canvas/index.js` | 200，26753 字节 |
| `.../manifest.json` `.../lib/marker.js` `.../lib/nai-api.js` `.../lib/settings.js` `.../style.css` | 200 |
| `/lib/jszip.min.js` | 200，97630 字节，动态 import 的目标路径真实可达 |
| DOM 挂载点 `#extensions_settings2` | 存在于 `index.html` L5778 |
| 全局 `$`(jQuery 3.5.1) / `toastr` | `index.html` L8188 / L8195 以普通 script 先于模块加载 |

### 14.4 ⚠️ 尚未完成的验证（留给下一步）

- **浏览器中打开抽屉查看 UI**：本机未安装浏览器自动化（`agent-browser` 无法安装，bash 环境损坏），
  故"抽屉展开 + 设置项落盘 + 图标/排版"需**人工在酒馆中点击确认**。
- **真实出图**：需一个运行中的 NAI 协议服务 + 上游。本轮无可用上游，ZIP 链路由本地假服务验证
  （较真实上游更可控，但无法获得真实图片）。
- 验收清单中标记为"待人工验证"的项均属此两类。

---

## 十五、验收清单状态（对应第十章逐条）

| # | 验收项 | 状态 | 说明 |
|---|---|---|---|
| 1 | 扩展在面板出现，抽屉可展开（自启动生效） | 待人工验证（半自动） | 服务端已发现 + 静态资源 200 + 挂载点存在；**抽屉展开需人眼确认** |
| 2 | 设置项可保存，刷新后保持 | 待人工验证 | 走 `extension_settings.v_canvas` + `saveSettingsDebounced`，逻辑同 V.Adapter；需人工确认 |
| 3 | 代码中搜不到任何硬编码模型名 | 已验证 | `model` 默认 `''`，仅 placeholder |
| 4 | 代码中**没有** OpenAI 兼容直连的实现 | 已验证 | 仅有 `${base}/ai/generate-image` |
| 5 | 图**就地插在该段落正下方** | 已验证 | 单测断言输出恰好是 `A\n\n![Illustration](url)\n\nB\n[ILLUST: two…]` |
| 6 | 图**独占一行**（无浮动、无环绕） | 已验证 | `style.css` 首条规则 `display:block; max-width:100%; height:auto; margin:8px auto` |
| 7 | 首回复 / 续写 / swipe 都能触发 | 已验证 | `MESSAGE_RECEIVED` 全 type 覆盖；`continue/append` 由 `effectiveSource` 处理；swipe 由 `MESSAGE_SWIPED` + 酒馆 `extra` 还原兜住 |
| 8 | 流式输出正常工作 | 已验证 | 代码路径一致（`onFinishStreaming → finalizeIntermediaryMessage → emit`），并额外处理了 `display_text` 遮蔽流式内容的问题（13.3①） |
| 9 | 同一消息不重复生图 | 已验证 | `illust_done` + `inFlight` + 4s 冷却 |
| 10 | 刷新 / 重开聊天后插图仍在且位置正确 | 已验证 | `display_text` 随 `extra` 落聊天记录；另有 `rehydrateAll` 兜底 |
| 11 | NAI ZIP 能正确解压显示 | 已验证 | 单测用真 ZIP 跑通（`JSZip.loadAsync` → base64 → 与 PNG 字节逐字节相等） |
| 12 | 上游报错时 loading 消失、弹窗、记录干净 | 已验证 | 无 loading 落盘；`toastr.error` 透传上游 message；失败时标记原样保留 |
| 13 | 消息文本里没有超长 base64 | 已验证 | 仅写入 `/api/images/*.png` URL |

图例：已验证 = 已被代码与自动化验证覆盖；待人工验证 = 逻辑就位，需人工在酒馆中确认（见 14.4）。
